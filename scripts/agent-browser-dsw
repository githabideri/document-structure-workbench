#!/usr/bin/env bash
set -euo pipefail

runtime_dir="/tmp/dsw-agent-browser"
mkdir -p "$runtime_dir"
chmod 700 "$runtime_dir"
export AGENT_BROWSER_SOCKET_DIR="$runtime_dir"
export AGENT_BROWSER_NAMESPACE="dsw-workbench"
exec agent-browser "$@"
